# OS
.DS_Store
Thumbs.db

# Frontend
node_modules/
dist/

# Python
__pycache__/
*.pyc
*.pyo
backend/venv/
backend/venv_old/
backend/venv_*/

# Environment / secrets
.env
.env.local
.env.production

# SQLite database (dev only — PG is primary)
backend/liquifi.db

# PyTorch model checkpoints (large binaries — train locally)
backend/models/*.pt
backend/models/*.npz

# Scraped data & cache (ephemeral, rebuilt by scrapers)
backend/data/collected/
backend/data/_cache/

# Live + training CSVs (rebuilt by training pipeline)
backend/seed_data/live_snapshots.csv
backend/seed_data/training_rates.csv
backend/seed_data/training_rates_holdout.csv

# Logs & metrics
backend/logs/
backend/metrics/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Review / debug archives (large, not source)
review_files12/
review_kimi_debug/
review_kimi_update/
*.zip

# Pytest / coverage
.pytest_cache/
htmlcov/
.coverage

# Electron / desktop build
electron/icon.icns
electron/*.js
electron/*.js.map
dist-electron/
release/
build-backend/
*.dmg
*.blockmap

# Legacy monolithic JSX
liquifi-ultimate.jsx

# Screenshots (referenced from README, keep in screenshots/ dir)
dashboard-screenshot.png

# Generated icon PNGs
public/icon-192x192.png
public/icon-512x512.png
public/apple-touch-icon-180x180.png
